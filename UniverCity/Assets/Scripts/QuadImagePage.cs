using UnityEngine;
using System.Collections;

public class QuadImagePage : MonoBehaviour 
{
    public Vector2 maxSize = new Vector2(1025.0f, 818.0f);

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
