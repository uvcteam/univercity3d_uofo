using UnityEngine;
using System.Collections;

public class TripleImagePage : MonoBehaviour 
{
    public Vector2 maxSize = new Vector2(682.0f, 1636.0f);

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
